<html>
    <head>
        
        <link rel="stylesheet" type="text/css" href="/bootstrap/dist/css/bootstrap.min.css">
    </head>
    <body>
        <div class="container">
            <div class="container-fluid py-5">
                <h1>Importing Master Data</h1>
                <% 
                if (typeof status != 'undefined') { %>
                    <%= status %>
                <%
                }
                %>
            </div>
            <div class="container-fluid py-5">
                <h3>make and import form for user to import cabang.csv</h3>
                <p class="warning">Warning : upload akan menghapus data lama.</p>
                <form action="/import/cabang" method="post" class="form" enctype='multipart/form-data'>
                    <div class="mb-3">
                        <label for="cabang" class="form-label">File Cabang.csv : kode, alamat, kota</label>
                        <input type="file" class="form-control" name="cabang">
                        <button type="submit" class="btn btn-primary mt-3">SUBMIT</button>
                    </div>
                </form>

                <h3>make and import form for user to import user.csv</h3>
                <p class="warning">Warning : upload akan menghapus data lama. password akan diubah menjadi standard '123456'</p>
                <form action="/import/user" method="post" class="form" enctype='multipart/form-data'>
                    <div class="mb-3">
                        
                        <label for="user" class="form-label">File User.csv : UserID; nama; Job; Admin; branchID; BranchCode; Branch</label>
                        <input type="file" class="form-control " name="user">
                        <button type="submit" class="btn btn-primary mt-3">SUBMIT</button>
                    </div>
                </form>

            </div>
    
        </div>
        <script type="text/javascript" src="/bootstrap/dist/js/bootstrap.min.js"></script>
    </body>
</html>